databaseChangeLog:

  - changeSet:
      id: 5-alter-user-password-reset
      author: bhargav
      changes:
        - addColumn:
            tableName: user_password_reset
            columns:
              - column:
                  name: id
                  type: UUID
                  defaultValueComputed: gen_random_uuid()
                  constraints:
                    primaryKey: true
                    nullable: false